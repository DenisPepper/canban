import React from "react";

const appStyles: React.CSSProperties = {
    backgroundColor: "red",
};

export const App = () => {
    return (
        <div style={appStyles}>
            kanban study project
        </div>
    );
}
